# Task 192: Decide — Introduce Pinia State Store — Progress

Type: refactoring

## Spec
- [x] spec

## Fix

### Step 1: Decision — adopt Pinia (auth/session store) or keep local refs
- [ ] design (write-up our approach + .claude constraints + senior remark + pros/cons → decide; ADR if adopted; coordinate with I1 / Story 3)

### Step 2: Implementation (only if adopted)
- [S] (steps defined after the Step 1 decision — store + router guard + move 401 redirect)
