# UI Tests — Email Integration

No UI test scenarios — this story has no frontend. The activation link in the email targets Story 1's existing frontend activation page, which is covered by Story 1's UI tests.
