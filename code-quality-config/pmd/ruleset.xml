<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="rpm-ddd PMD rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

  <description>
    Project PMD ruleset: all 8 PMD Java categories enabled. Violations are burned down in batches
    (see Task 244) — rules owned by Spotless/Checkstyle/SpotBugs or contradicting project conventions
    are excluded/configured below; the pom.xml maxAllowedViolations ceiling ratchets down per batch.
  </description>

  <rule ref="category/java/bestpractices.xml"/>


  <rule ref="category/java/codestyle.xml">
    <!-- Batch 1: no tool in the stack enforces `final` modifiers (Checkstyle has no
         FinalParameters/FinalLocalVariable, Palantir format does not add them); the project
         did not adopt the convention. Disabled rather than churn 800 mechanical edits. -->
    <exclude name="MethodArgumentCouldBeFinal"/>
    <exclude name="LocalVariableCouldBeFinal"/>
  </rule>
  <!-- Override: the default cap of 4 is too low for test DSLs that lean on static imports. -->
  <rule ref="category/java/codestyle.xml/TooManyStaticImports">
    <properties>
      <property name="maximumStaticImports" value="7"/>
    </properties>
  </rule>


  <rule ref="category/java/design.xml"/>


  <rule ref="category/java/documentation.xml">
    <!-- Configured in Checkstyle -->
    <exclude name="CommentRequired"/>
    <!-- Batch 2: comment line length is owned by Checkstyle LineLength=120 (my_checks.xml,
         applies to comments too) and .editorconfig=120; PMD's 80-char default conflicts with the
         adopted 120. The 6-line cap conflicts with the project rule to preserve Javadoc/comments. -->
    <exclude name="CommentSize"/>
  </rule>


  <rule ref="category/java/errorprone.xml"/>


  <rule ref="category/java/multithreading.xml"/>


  <rule ref="category/java/performance.xml"/>


  <rule ref="category/java/security.xml"/>

</ruleset>
